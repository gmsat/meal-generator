import React from 'react';
import {BottomNavigation} from "@mui/material";

const Footer = () => {
    return (
        <BottomNavigation sx={{height: 150, backgroundColor: "primary.alt"}}></BottomNavigation>
    );
};

export default Footer;